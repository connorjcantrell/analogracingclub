{
  "name": "analogracingclub",
  "version": "2.0.0",
  "private": true,
  "description": "Analog Racing Club — iRacing league site (analogracingclub.com). SvelteKit + MongoDB, admin upload of iRacing event-result JSON, computed standings.",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node build",
    "ingest": "node src/lib/server/import/cli.js",
    "test": "node --test"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.5.7",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.0",
    "vite": "^8.2.2"
  }
}
