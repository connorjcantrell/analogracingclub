{
  "name": "analogracingclub",
  "version": "1.0.0",
  "private": true,
  "description": "Analog Racing Club — iRacing league site (analogracingclub.com). Node + MongoDB, admin upload of iRacing event-result JSON, computed standings.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "ingest": "node src/import/cli.js",
    "test": "node --test"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  }
}
