node_modules/
.env
.env.local
.claude/settings.local.json
.DS_Store

# Local dev-only Docker overrides (must not reach deploys)
docker-compose.override.yml

# Uploaded race photos (user content)
public/assets/rounds/
